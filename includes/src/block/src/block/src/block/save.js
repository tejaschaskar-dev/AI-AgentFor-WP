/**
 * WP Agent AI – Block Save Component
 *
 * This block is editor-only (a generation UI).
 * All output is inserted as separate native blocks,
 * so save() correctly returns null.
 *
 * @package WpAgentAi
 */

export default function Save() {
	return null;
}
